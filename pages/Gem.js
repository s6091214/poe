import React from "react";

const Gem = () => {
  return (
    <div
      className={
        "flex justify-center flex-col items-center bg-black bg-fixed bg-poe h-screen bg-no-repeat bg-cover"
      }
    >
      Gem
    </div>
  );
};

export default Gem;
